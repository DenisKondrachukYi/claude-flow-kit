{
  "$schema": "https://json.schemastore.org/mcp.json",
  "description": "Optional MCP servers. Copy entries you need into .mcp.json, or merge with jq.",
  "mcpServers": {
    "serena": {
      "description": "Semantic code search (IDE-like). Install: uvx --from git+https://github.com/oraios/serena serena",
      "command": "uvx",
      "args": ["--from", "git+https://github.com/oraios/serena", "serena"]
    },
    "spec-workflow": {
      "description": "Full SDD workflow with web dashboard + VSCode extension (Pimzino)",
      "command": "npx",
      "args": ["-y", "@pimzino/spec-workflow-mcp@2.2.5", "."]
    },
    "mcpvault": {
      "description": "Obsidian vault access (AST-aware). Replace VAULT_PATH with the absolute path to your vault.",
      "command": "npx",
      "args": ["-y", "@bitbonsai/mcpvault@0.11.0", "/Users/YOU/path/to/ObsidianVault"]
    },
    "memory": {
      "description": "Persistent memory via knowledge graph (Anthropic official)",
      "command": "npx",
      "args": ["-y", "@modelcontextprotocol/server-memory@2026.1.26"]
    },
    "obsidian-claude-code": {
      "description": "Obsidian plugin with WebSocket bridge on port 22360. Install the plugin in Obsidian first; npm package not yet available.",
      "command": "npx",
      "args": ["-y", "obsidian-claude-code-mcp"]
    }
  }
}
