## What

<!-- One-paragraph summary of the change. -->

## Why

<!-- Motivation: which user pain or feature gap this addresses. -->

## How tested

- [ ] `npm run smoke-test` passes
- [ ] `npm run lint:shell` passes
- [ ] `npm run lint:json` passes
- [ ] Manually tested with `node bin/cli.js init /tmp/test`

## Checklist

- [ ] Updated `CHANGELOG.md` (`[Unreleased]` section)
- [ ] Updated README if user-facing
- [ ] Updated `CONTRIBUTING.md` if dev workflow changed
- [ ] Added/updated `FRAMEWORK_OWNED` in `src/lib/upgrade.js` if added template files
